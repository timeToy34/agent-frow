# Corsair iCUE SDK (redistributable; obtained per-machine, not committed)
/iCUESDK/

# Local environment and secrets
*.local
.env
.env.*

# Editor and OS noise
.DS_Store

# Build output.
/target
# Built packages.
/dist
# Development scratch: a screenshot and the script that takes it.
/window.png
/shot.ps1
# The Keychron firmware build: a clone of Keychron's fork and the built image.
/firmware/keychron-ultra/zmk/
/firmware/keychron-ultra/out/
# The demo video: too big for the repository, and a README only plays one
# served by GitHub's own uploader. Kept beside the checkout, never committed.
*.mp4
# Windows' download-origin sidecars.
*:Zone.Identifier
